{
  "name": "customer-manager",
  "version": "1.0.0",
  "description": "Quản Lý Khách Hàng - Cloudflare KV Native iOS App",
  "main": "index.js",
  "scripts": {
    "build": "node -e \"const fs = require('fs'); if (!fs.existsSync('www')) fs.mkdirSync('www'); fs.copyFileSync('index.html', 'www/index.html'); console.log('✓ Assets built to www/');\""
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
